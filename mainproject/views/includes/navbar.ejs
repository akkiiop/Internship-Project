<nav class="navbar navbar-expand-md bg-body-tertiary border-bottom sticky-top">
     <!-- sticky-top is for position sticky -->
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/listings">All Destinations</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/listings/new">Add New Destination</a>
          </li>
        </ul>
      </div>
     
    </div>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav ms-auto">
        <!-- currUser is defined in app.js, it is using method req.user to check user is loggedin or not -->
        <% if(!currUser) { %>  
        <a class="nav-link"  href="/signup">Sign up</a>
        <a class="nav-link" href="/login">Log in</a>
        <% } %> <% if(currUser) { %>
        <a class="nav-link" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>
  </nav>